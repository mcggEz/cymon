# Backend API base URL.
# Leave empty in dev — Vite proxies /api/* to http://localhost:4000 (see vite.config.js).
# In production, set this to your deployed backend's origin, e.g.
#   VITE_API_BASE_URL=https://cymon-backend.vercel.app
VITE_API_BASE_URL=
